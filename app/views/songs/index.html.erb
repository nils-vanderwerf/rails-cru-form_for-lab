<h1> My Songs </h1>
<% @songs.each do |song|%>
    <p><%= song.name %></p><br>
    <%= (link_to song.artist.name, artist_path(song.artist)) if song.artist %><br>
    <%= (link_to song.genre.name, genre_path(song.genre)) if song.genre %>
<% end %>